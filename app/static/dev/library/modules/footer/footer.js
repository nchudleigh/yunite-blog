(function(window, document, location, navigator, $, angular, undefined) {
    'use strict';

    angular.module('postster', [
        'postster.controller'
      , 'postster.model'
    ])
    
;}(window, document, location, navigator, jQuery, angular, undefined));